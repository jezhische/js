Main
1.Создать функцию которая вернет массив отсортированный по-возрастанию и по-убыванию цены,
в зависимости от того какой параметр указан
getSortArrByPrice('ascending');
getSortArrByPrice('descending');
2.Создать функцию которая добавит новую пиццу в массив pizzaList 
созданную функцией-конструктором new Pizza(id, img, name, [...compositionIds]).
Для начала вам нужно создать функцию конструктор Pizza. Composition свойство должно содержать
массив с названиями продуктов взятыми по id из массива compositionList. Цена и калории должны 
расчитываться исходя из добавленных продуктов (caloricity = calloriesCount + 1000, price = priceCount + 1000), 
То есть когда вы создаете пиццу вы получаете четвертым параметром массив id compositionList,
а внутри функции конструктора перебираете массив compositionList, фильтруете по id и производите
расчеты.

Удачи)00)0)))0)00)0)))

Таск вместе с JavaScript и HTML файлами пушим в свой созданные репозиторий.
Репозиторий должен называться Module1_HomeWork4;
